/**
 * Helper function to log metadata
 *
 * @param metadata - Metadata object returned by operations
 */
export default async function logMetadata(metadata: any) {
    console.log('📄 Metadata:', metadata)
}